import { InstanceState } from "../FBASInstance";
import Slices from '../Slice';

export const determineQuorum = (state: InstanceState, slices: Slices) => {
    slices.slices.forEach(slice => {
         
    })
}